# 编排规则

## 目标

本项目由主代理负责编排，不由主代理直接参与实际编码。实现工作交给子代理，并按实施计划分阶段推进。

## 分阶段推进

- 按实施计划的 Task 1–9 分阶段执行。
- 一个阶段完成并通过该阶段的验证后，才进入下一阶段。
- 每个阶段都必须核对修改范围、提交内容、测试证据和是否越过 M1 边界。
- 不提前实现实施计划明确排除的 M2–M5 功能。

## 子代理规则

- 实施和修复统一派遣自定义代理 `luna_worker`。
- 阶段实施完成后，必须派遣一个新的、独立的 `luna_worker` 进行对抗性审查，不能默认由实施该阶段的代理自审替代。
- 对抗性审查至少检查：需求符合性、实施计划符合性、变更范围、测试充分性、提交内容、第三方文件完整性和安全边界。
- 审查发现问题后，修复完成必须重新审查；未通过不得进入下一阶段。

## 问题处理

- 对于明确、局部、低风险且不改变方向的问题，直接让子代理做最简修复，禁止顺便重构或扩大范围。
- 对于涉及需求解释、架构方向、依赖或版本更换、真实数据结果、权限与安全边界、第三方来源或实施停止规则的问题，主代理必须暂停推进并向用户询问。
- 需要用户决策时，主代理必须给出若干可行候选方案、说明各自影响，并等待用户选择；不得擅自替用户决定。
- 触发实施计划停止规则时，必须保留完整错误和证据，停止当前阶段，不通过重试、绕过检查或扩大修改范围掩盖问题。

## 决策记录

- 首次出现需要用户决定的问题时创建项目根目录的 `mistake.md`；此前不创建空文件。
- 每次需要用户决策的事项都追加记录，至少包含：
  - 当前进度
  - 相关提交（提交哈希；若未提交则明确写明）
  - 问题
  - 候选方案
  - 用户最终选择
  - 为什么最终选择该方案
  - 决策后的理解和执行边界
- 用户未作出选择前不得继续执行受该决策影响的工作。

## 交付门槛

- 每阶段交付必须提供实际命令、测试结果、审查结果和提交信息。
- 只有满足 M1 最终完成门槛，才允许开始 M2。
- 不得把 mock、静态检查或未验证的推测报告为真实 PostgreSQL/dbt 验收结果。
- 不得将未实现的功能写成已完成，也不得把单案例结果表述为通用准确率或生产能力结论。
